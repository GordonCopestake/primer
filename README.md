# Primer

Minimal local-first implementation scaffold based on `spec.txt`.

This repo currently implements the core v1 foundation:

- static web shell with PWA manifest and service worker
- local config and state model helpers
- deterministic curriculum and mastery engine
- capability detection
- safe fallback scene
- strict scene validation and interpreter guards

Run tests with:

```bash
npm test
```
