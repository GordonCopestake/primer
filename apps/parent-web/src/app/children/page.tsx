import { ParentAreaShell } from "../../features/parent-area";

export default function ChildrenPage() {
  return (
    <ParentAreaShell
      title="Child profiles"
      description="Manage learner profiles stored locally on this device."
    />
  );
}
