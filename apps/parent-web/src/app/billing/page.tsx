import { ParentAreaShell } from "../../features/parent-area";

export default function BillingPage() {
  return (
    <ParentAreaShell
      title="Plan and billing"
      description="View local plan metadata. Cloud billing workflows are not part of local-only MVP flows."
    />
  );
}
